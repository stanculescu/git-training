server:
  port: 9090
  servlet:
    context-path: /tmd

spring:
  application:
    name: TMD
  datasource:
    url: jdbc:h2:file:./database
    username: sa
    password:
    driver-class-name: org.h2.Driver
  profiles:
    active: dev

logging:
  level:
    root: info